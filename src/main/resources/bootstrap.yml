server:
  port: 8001
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: java-tree
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        username: nacos
        password: nacos